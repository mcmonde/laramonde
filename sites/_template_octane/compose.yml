# Instantiated by ./dock new-app <name> --octane

x-app: &app
  image: ${PHP_OCTANE_IMAGE:-multiapp-php-octane}:${PHP_VERSION:-8.4}
  build:
    context: ./php-octane
    dockerfile: Dockerfile
    args:
      PHP_VERSION: ${PHP_VERSION:-8.4}
      APP_UID: ${APP_UID:-1000}
      APP_GID: ${APP_GID:-1000}
  restart: unless-stopped
  working_dir: ${APP_CODE_PATH_CONTAINER:-/var/www/apps}/__APP__
  user: "${APP_UID:-1000}:${APP_GID:-1000}"
  volumes:
    - ${APP_CODE_PATH_HOST:-./apps}/__APP__:${APP_CODE_PATH_CONTAINER:-/var/www/apps}/__APP__
  environment:
    APP_NAME: ${APP_NAME:-__APP__}
    APP_URL: ${APP_URL:-http://__DOMAIN__}
    APP_RUNTIME: octane
    OCTANE_SERVER: swoole
    DB_CONNECTION: pgsql
    DB_HOST: postgres
    DB_PORT: "5432"
    DB_DATABASE: ${DB_DATABASE:-__DBIDENT__}
    DB_USERNAME: ${DB_USERNAME:-__DBIDENT__}
    DB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in sites/__APP__/defaults.env}
    REDIS_CLIENT: phpredis
    REDIS_HOST: redis
    REDIS_PORT: "6379"
    REDIS_PASSWORD: ${REDIS_PASSWORD:?Set REDIS_PASSWORD — run ./dock setup}
    REDIS_PREFIX: ${REDIS_PREFIX:-__APP___}
    CACHE_STORE: redis
    SESSION_DRIVER: redis
    QUEUE_CONNECTION: redis
    # Scout/Mail follow COMPOSE_PROFILES (synced by ./dock setup|resources apply)
    SCOUT_DRIVER: ${SCOUT_DRIVER:-null}
    SCOUT_PREFIX: ${SCOUT_PREFIX:-__APP___}
    MEILISEARCH_HOST: ${MEILISEARCH_HOST:-}
    MEILISEARCH_KEY: ${MEILI_MASTER_KEY:-}
    MAIL_MAILER: ${MAIL_MAILER:-log}
    MAIL_HOST: ${MAIL_HOST:-}
    MAIL_PORT: ${MAIL_PORT:-2525}
    BROADCAST_CONNECTION: reverb
    REVERB_APP_ID: ${REVERB_APP_ID:-__APP__}
    REVERB_APP_KEY: ${REVERB_APP_KEY:?Set REVERB_APP_KEY}
    REVERB_APP_SECRET: ${REVERB_APP_SECRET:?Set REVERB_APP_SECRET}
    REVERB_HOST: 0.0.0.0
    REVERB_PORT: "8080"
    REVERB_SCHEME: http
  networks:
    - backend
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy
  security_opt:
    - no-new-privileges:true
  cap_drop:
    - ALL
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"

services:
  __APP__-octane:
    <<: *app
    command: ${OCTANE_COMMAND:-php artisan octane:start --server=swoole --host=0.0.0.0 --port=8000 --workers=4 --task-workers=2 --max-requests=500}
    networks:
      - frontend
      - backend
    expose:
      - "8000"
    healthcheck:
      test: ["CMD", "php", "-r", "exit(@fsockopen('127.0.0.1', 8000) ? 0 : 1);"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: ${OCTANE_CPUS:-2}
          memory: ${OCTANE_MEMORY:-1G}

  __APP__-queue:
    <<: *app
    command: ${APP_QUEUE_COMMAND:-php artisan queue:work --sleep=3 --tries=3 --max-time=3600}
    deploy:
      resources:
        limits:
          cpus: ${QUEUE_CPUS:-1}
          memory: ${QUEUE_MEMORY:-512M}

  __APP__-scheduler:
    <<: *app
    command: php artisan schedule:work
    deploy:
      resources:
        limits:
          cpus: ${SCHEDULER_CPUS:-0.5}
          memory: ${SCHEDULER_MEMORY:-256M}

  __APP__-reverb:
    <<: *app
    command: php artisan reverb:start --host=0.0.0.0 --port=8080
    networks:
      - frontend
      - backend
    expose:
      - "8080"
    deploy:
      resources:
        limits:
          cpus: ${REVERB_CPUS:-1}
          memory: ${REVERB_MEMORY:-512M}
