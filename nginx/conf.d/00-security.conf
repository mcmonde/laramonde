# Shared security headers + hidden-file deny. Included by every site via
# `include /etc/nginx/conf.d/00-security.conf;` is not automatic — sites
# inherit nothing. This file is a snippet referenced from site templates.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
